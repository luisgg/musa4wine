#!/bin/sh -e

if ! getent passwd musa4wine >/dev/null; then
   adduser --quiet --system --home /var/lib/musa4wine --shell /bin/sh --disabled-login  --group musa4wine
fi

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0

